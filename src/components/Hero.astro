<section class="hero">
  <div class="hero_div">
    <video autoplay muted loop class="hero-video">
      <source src="hero.mp4" type="video/mp4" />
    </video>
  </div>
  <div class="hero-content">
    <img class="hero_img" src="herologo.png" alt="Bareen x CPH Marathon" />
  </div>
</section>

<style>
  .hero {
    margin: 0;
    padding: 0;
    height: 100%;
    display: grid;
    grid-template-rows: 1fr 1fr 1fr;
    grid-template-columns: 1fr;
  }

  .hero_div {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    grid-row: 1/4;
    grid-column: 1;
  }

  .hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100vw;
    object-fit: cover;
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2; /* Sørger for, at indhold vises over videoen */
    grid-row: 2/3;
    grid-column: 1;
    place-self: center;
    justify-content: center;
    justify-items: center;
    align-items: center;
    align-content: center;
    align-self: center;
  }

  @media (max-width: 800px) {
    .hero-video {
      width: 100vw;
      height: 100vh;
      background-size: contain;
      object-fit: cover;
      display: block;
    }

    .hero_div {
      position: relative;
      width: 100%;
      overflow: hidden;
      display: inline-grid;
      align-items: center;
      justify-content: center;
      grid-row: 1/4;
      grid-column: 1;
    }

    .hero_img {
      position: relative;
      width: 100%;
      overflow: hidden;
      display: inline-grid;
      align-items: center;
      justify-content: center;
      grid-row: 1/4;
      grid-column: 1;
    }
  }
</style>
